<?php
    return [
        // 是否开启路由
        'url_route_on'           => true,

        // 是否强制使用路由
        'url_route_must'         => false,

        // 默认返回类型json
        'default_return_type'    => 'json',

        // 定义app签名加密前缀字符串
        'app_sign_entry_string'  => 'sbkz',
    ];